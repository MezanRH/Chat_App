import React from 'react'

const SubHadding = ({text,className}) => {
  return (
    <h3 className={`font-inter text-2xl font-bold text-2nd capitalize ${className}`}>{text}</h3>
  )
}

export default SubHadding